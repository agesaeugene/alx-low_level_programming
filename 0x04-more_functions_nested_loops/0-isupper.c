#include "main.h"

/**
 * _isupper - checks if the letter is uppercase
 *
 * @x: The numbe rto be checked
 *
 * Return: 1 for upper case letter  or 0 for other
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (1);
}

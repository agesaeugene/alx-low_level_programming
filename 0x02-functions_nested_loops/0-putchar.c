#include <stdio.h>
/**
 * main - program to print text
 *
 * Return: Always (0)
 */
int main(void)
{
	int a;
	char c[] = "_putchar";

	for (a = 0; a <= 7; a++)
	{
		putchar(c[a]);
	}
	putchar('\n');
		return (0);
}

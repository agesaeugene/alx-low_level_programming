#include <stdio.h>
/**
 * main - name if printed and a new line returned
 * @argc: argument count
 * @argv: arguments  vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
